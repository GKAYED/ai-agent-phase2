# Quick Start

## Docker (recommended)

```powershell
# Build and start
docker-compose up --build
# Open in browser
Start-Process http://localhost:3000
```

Data persists in the `ai-agent-data` volume.
